services:
  app:
    build: .
    container_name: glitch-kitchen
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./public:/app/public
    restart: unless-stopped
    command: node dist/index.js
